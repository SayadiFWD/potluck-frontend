// Libraries
import React from "react";

const PotluckInfo = props => {
	return (
		<div>
      <p>Event Name: {props}</p>
      <p>Date: {props}</p>
      <p>Time: {props}</p>
      <p>Address: {props}</p>

		</div>
	);
};

export default PotluckInfo;
